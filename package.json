{
  "name": "oncopaper-radar",
  "version": "2.1.0",
  "private": true,
  "description": "Cloudflare Workers AI + Workflows + D1 for oncology literature discovery",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "check": "node --check src/ai.js && node --check src/query.js && node --check src/radar.js && node --check src/utils.js && node --check src/search.js && node --check src/storage.js && node --check src/migrate.js && node --check src/index.js && node --check public/app.js",
    "test": "node --test",
    "db:init:local": "wrangler d1 execute oncopaper-radar --local --file=./schema.sql",
    "db:init:remote": "wrangler d1 execute oncopaper-radar --remote --file=./schema.sql",
    "db:show:local": "wrangler d1 execute oncopaper-radar --local --command=\"SELECT * FROM settings;\"",
    "db:show:remote": "wrangler d1 execute oncopaper-radar --remote --command=\"SELECT * FROM settings;\""
  },
  "devDependencies": {
    "wrangler": "^4.26.0"
  }
}
